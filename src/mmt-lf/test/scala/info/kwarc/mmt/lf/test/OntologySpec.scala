package scala.info.kwarc.mmt.api.test

import info.kwarc.mmt.api._
import info.kwarc.mmt.api.frontend._

import org.scalatest._
/*
class OntologySpec extends FlatSpec with Matchers {
  val controller = new Controller()
  //using the same test archive as for WebServer -- to be renamed 
  val testLocation = "src/test/resources/WebServerTest/"
  //controller.handleLine("log console")
  //controller.handleLine("log+ archive")
  //controller.handleLine("log+ build")
  //controller.handleLine("log+ controller") 
  
  controller.handleLine("archive add " + testLocation)
  //controller.handleLine("build test mmt-omdoc")
  
  "Loading relation info" should "not throw an error" in {
    controller.handleLine("archive test relational")
  }
  
  it should "make relational information available for queries" in {
    pending
    //controller.memory.ontology.query(start-path, relationexp)
  }
  
}
*/