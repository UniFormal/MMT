HReqHeader.scala:122: instead of 

     if (extParts.size == 2 && extParts(1).trim.size > 0) anUriExt = Some(extParts(1).trim)
     
have
 
     if (extParts.size == 2) anUriExt = Some(extParts(1).trim)

FR: not sure what this was about but probably doesn't hurt to redo this change
----------
Revision 576: bug fix
----------
Revision 1193: All println have been turned into function calls so that the output method can be overridden.