%* document comment
   @author alin iacob 
   @date january 2011 *%

%namespace "http://cds.omdoc.org/a/blah".
%namespace a = "yadda".

%{some comment}%
%* test signature *% 
%sig alpha∨ =
{
   %* test declaration *%
   %{ nothing }%
   b : type.
   d : b.
   c : b.
   a : b = c.
}.%namespace "../blahblah→".
%namespace ccc = "a/blah".
%sig beta = {
   %struct a : ccc.alpha∨ %open.
   }.%%
