Archives should always be open initially

URI in main should not line-break

Some times the content of Main is shown multiple times.

When using a context menu function such as "show declaration", the method createInlineBox is called.
It currently inserts the box with the wrong height and scrolls wrongly.

------------------------
long-term

search field in Archives

better handling of comments

integration with JOBAD side bar