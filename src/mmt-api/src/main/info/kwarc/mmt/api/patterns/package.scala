package info.kwarc.mmt.api

import patterns._

/**
  * Declaration patterns in the sense of Horozal's PhD thesis, realized as two special cases of structural features:
  * - [[Pattern]] for the patterns (elaborates to nothing)
  * - [[Instance]] for the instances of patterns (elaborates by looking up the pattern)
  */
package object patterns {
}
