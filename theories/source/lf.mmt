%namespace "http://cds.omdoc.org/foundations".

%sig Typed = {
   kind.
   type.
}.

%sig LF = {
   %include Typed.
   
   pi ## { 1 }.
   lambda ## [ 1 ].
   @.
   arrow # 1/->,-10.
   
   %%%% 
   omitted.
   implicit_Pi.
   implicit_lambda.
   %%%%
   
   %% %pattern type = {a: type.}.

   %% %pattern constant = [a:tp] {c: a.}.
}.

%sig Errors = {
   missing # ( _ : 1 ).
   illtyped # ( 1 : 2 ).   
}