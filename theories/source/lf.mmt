%namespace "http://latin.omdoc.org/foundations".

%sig Typed = {
   type.
   kind.
}.

%sig LF = {
   %include Typed.
   
   pi ## { 1 }.
   lambda ## [ 1 ].
   @.
   -> # 1/->.
   
   %pattern type = {a: type.}.

   %% %pattern constant = [a:tp] {c: a.}.
}.