%namespace "http://cds.omdoc.org/foundational".

%sig Typed = {
   kind.
   type.
}.

%sig LF = {
   %include Typed.
   
   pi ## { 1 }.
   lambda ## [ 1 ].
   @.
   arrow # 1/->.
   
   %pattern type = {a: type.}.

   %% %pattern constant = [a:tp] {c: a.}.
}.