%namespace "http://cds.omdoc.org/foundations".

%sig Typed = {
   kind.
   type.
}.

%sig LF = {
   %include Typed.
   
   Pi # { 1:_ } -2 prec 10000.
   lambda # [ 1:_ ] -2 prec 10000.
   @.
   arrow # 1→… prec 9999.
   
  %% %pattern type = {a: type.}.

   %% %pattern constant = [a:tp] {c: a.}.
}.