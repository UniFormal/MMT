%namespace "http://cds.omdoc.org/foundations".

%sig Typed = {
   kind.
   type.
}.

%sig LF = {
   %include Typed.
   
   pi # { 1:_ } -2,1000.
   lambda # [ 1:_ ] -2,1000.
   @.
   arrow # 1→….

   omitted.
   implicit_Pi.
   implicit_lambda.
   
  %% %pattern type = {a: type.}.

   %% %pattern constant = [a:tp] {c: a.}.
}.

%sig Errors = {
   missing # ( ? : 1 ).
   illtyped # ( 1 : 2 ).   
}